<p style="color: green"><%= notice %></p>

<h1>Bands</h1>

<%= form_with(url: bands_path, method: :get, data: {controller: "search-form", turbo_frame: "bands", turbo_action: "advance"}) do |form| %>
  <%= form.label :query, "Nome:", class: "block mb-2" %>
  <%= form.text_field :query, data: {action: "input->search-form#search"} %>

  <%= form.label :query_1, "Idade:", class: "block mb-2" %>
  <%= form.text_field :query_1, data: {action: "input->search-form#search"} %>



<% end %>

<div>
 
  <%= render "bands", bands: @bands %>  
   
</div>